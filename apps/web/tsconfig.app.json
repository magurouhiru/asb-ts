{
  "extends": "@tsconfig/vite-react/tsconfig.json",
  "compilerOptions": {
    // 相対パスが嫌なので、 "@/..." で src 以下を参照できるようにする
    // ここ以外はデフォルト設定のまま
    "paths": {
      "@/*": ["./src/*"]
    },
    // "browser" を優先的に解決するようにする
    "customConditions": ["browser"]
  },
  "include": ["src"]
}
